"use client";

// หน้าดูรายชื่อผู้ใช้สำหรับ Advisor (read-only)
import { UserManagementDashboard } from "@/modules/user/components/UserManagementDashboard";

export default function AdvisorUsersPage() {
  return <UserManagementDashboard />;
}
